# ===================================================================
# 1. 보안 파일 (!!! 절대로 깃허브에 올리면 안 됩니다 !!!)
# ===================================================================
# 환경 변수 파일 (API 키, DB 비밀번호 등)
.env
.env.*

# 구글 클라우드 서비스 계정 키
gcloud-key.json


# ===================================================================
# 2. 의존성 폴더 (설치 파일)
# ===================================================================
# Node.js 의존성 폴더 (package.json으로 재설치 가능)
node_modules/

# Python 가상 환경 폴더 (requirements.txt로 재설치 가능)
venv/
.venv/


# ===================================================================
# 3. 빌드 결과물 (자동 생성 파일)
# ===================================================================
# 빌드/배포 폴더
dist/
release/
build/


# ===================================================================
# 4. 로그 및 임시 파일
# ===================================================================
# 로그 파일
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
build-log.txt
error.log

# 임시 오디오 저장 폴더 (server.js에서 사용)
temp_audio/

# WebDAV 서버 데이터 파일 (server.js에서 사용)
webdav-data.json
webdav-data.tmp.json

# Python 캐시 파일
__pycache__/
*.pyc


# ===================================================================
# 5. OS 및 에디터 설정 파일 (선택 사항)
# ===================================================================
# macOS
.DS_Store

# Windows
Thumbs.db

# VSCode, WebStorm 등 IDE 설정
.vscode/
.idea/

# Python 빌드 결과물 (PyInstaller 등)
build_py/
python-env/
python_worker/